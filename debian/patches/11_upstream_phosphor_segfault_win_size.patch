
# Source: https://bugzilla.redhat.com/show_bug.cgi?id=481146
# Debian: http://bugs.debian.org/cgi-bin/bugreport.cgi?bug=505379

Index: xscreensaver/hacks/phosphor.c
===================================================================
--- xscreensaver.orig/hacks/phosphor.c	2009-06-29 23:52:43.000000000 +0200
+++ xscreensaver/hacks/phosphor.c	2009-06-29 23:53:04.000000000 +0200
@@ -1353,6 +1353,7 @@
 	  if (state->pid)
 	    {
 	      waitpid(state->pid, NULL, 0);
+              state->pid = 0;
 	      fclose (state->pipe);
 	    }
 	  else
@@ -1436,7 +1437,7 @@
   resize_grid (state);
 
 # if defined(HAVE_FORKPTY) && defined(TIOCSWINSZ)
-  if (state->pid)
+  if (state->pid && state->pipe)
     {
       /* Tell the sub-process that the screen size has changed. */
       struct winsize ws;
